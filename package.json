{
  "name": "mern-app",
  "version": "1.0.0",
  "scripts": {
    "gdocs": "cd server && cd gdocs-api && concurrently \"node functions.js\" \"node index.js\"",
    "start-backend": "cd server && npm start",
    "start": "set NODE_ENV=development && concurrently \"npm run start-backend\" \"npm run react-dev\"",
    "react-dev": "cd client && npm start",
    "test": "cd server && npm test",
    "preinstall": "cd server && npm install",
    "install": "cd client && npm install",
    "build": "cd client && npm run build"
  },
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "devDependencies": {
    "prettier": "2.5.1"
  }
}


